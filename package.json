{
  "name": "@aklinker1/generate-changelog",
  "version": "0.1.0",
  "keywords": [
    "generate",
    "changelog",
    "ci",
    "conventional-commits",
    "git"
  ],
  "author": "Aaron Klinker <aaronklinker1@gmail.com>",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.cjs",
  "module": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "compile": "tsc --noEmit",
    "test": "vitest",
    "build": "pnpm build:package && pnpm build:cli && pnpm build:github-action",
    "build:package": "tsup src/index.ts --sourcemap --format cjs,esm --dts",
    "build:cli": "echo 'TODO'",
    "build:github-action": "echo 'TODO'",
    "prepack": "rimraf dist && pnpm build:package && pnpm build:cli"
  },
  "dependencies": {
    "simple-git": "^3.14.1"
  },
  "devDependencies": {
    "@types/node": "^18.7.18",
    "rimraf": "^3.0.2",
    "tsup": "^6.2.3",
    "typescript": "^4.8.3",
    "vite": "^3.1.2",
    "vitest": "^0.23.4"
  }
}
